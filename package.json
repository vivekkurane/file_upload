{
  "name": "file_upload_mean",
  "version": "1.0.0",
  "description": "Simple MEAN-style document upload app (Express + Mongoose + AngularJS)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client:install": "cd client && npm install",
    "client:start": "nodemon server.js && cd client && npx ng serve",
    "client:build": "cd client && npx ng build --configuration production"
    },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
